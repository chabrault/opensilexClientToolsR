# OpenSilex API
#
# No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
#
# OpenAPI spec version: INSTANCE-SNAPSHOT
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' CSVDuplicateURIError Class
#'
#' @field rowIndex 
#' @field colIndex 
#' @field header 
#' @field value 
#' @field previousRow 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
CSVDuplicateURIError <- R6::R6Class(
  'CSVDuplicateURIError',
  public = list(
    `rowIndex` = NULL,
    `colIndex` = NULL,
    `header` = NULL,
    `value` = NULL,
    `previousRow` = NULL,
    initialize = function(`rowIndex`, `colIndex`, `header`, `value`, `previousRow`){
      if (!missing(`rowIndex`)) {
        stopifnot(is.numeric(`rowIndex`), length(`rowIndex`) == 1)
        self$`rowIndex` <- `rowIndex`
      }
      if (!missing(`colIndex`)) {
        stopifnot(is.numeric(`colIndex`), length(`colIndex`) == 1)
        self$`colIndex` <- `colIndex`
      }
      if (!missing(`header`)) {
        stopifnot(is.character(`header`), length(`header`) == 1)
        self$`header` <- `header`
      }
      if (!missing(`value`)) {
        stopifnot(is.character(`value`), length(`value`) == 1)
        self$`value` <- `value`
      }
      if (!missing(`previousRow`)) {
        stopifnot(is.numeric(`previousRow`), length(`previousRow`) == 1)
        self$`previousRow` <- `previousRow`
      }
    },
    toJSON = function() {
      CSVDuplicateURIErrorObject <- list()
      if (!is.null(self$`rowIndex`)) {
        CSVDuplicateURIErrorObject[['rowIndex']] <- self$`rowIndex`
      }
      if (!is.null(self$`colIndex`)) {
        CSVDuplicateURIErrorObject[['colIndex']] <- self$`colIndex`
      }
      if (!is.null(self$`header`)) {
        CSVDuplicateURIErrorObject[['header']] <- self$`header`
      }
      if (!is.null(self$`value`)) {
        CSVDuplicateURIErrorObject[['value']] <- self$`value`
      }
      if (!is.null(self$`previousRow`)) {
        CSVDuplicateURIErrorObject[['previousRow']] <- self$`previousRow`
      }

      CSVDuplicateURIErrorObject
    },
    fromJSON = function(CSVDuplicateURIErrorJson) {
      CSVDuplicateURIErrorObject <- jsonlite::fromJSON(CSVDuplicateURIErrorJson)
      if (!is.null(CSVDuplicateURIErrorObject$`rowIndex`)) {
        self$`rowIndex` <- CSVDuplicateURIErrorObject$`rowIndex`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`colIndex`)) {
        self$`colIndex` <- CSVDuplicateURIErrorObject$`colIndex`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`header`)) {
        self$`header` <- CSVDuplicateURIErrorObject$`header`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`value`)) {
        self$`value` <- CSVDuplicateURIErrorObject$`value`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`previousRow`)) {
        self$`previousRow` <- CSVDuplicateURIErrorObject$`previousRow`
      }
    },
    fromJSONObject = function(CSVDuplicateURIErrorObject) {
      if (!is.null(CSVDuplicateURIErrorObject$`rowIndex`)) {
        self$`rowIndex` <- CSVDuplicateURIErrorObject$`rowIndex`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`colIndex`)) {
        self$`colIndex` <- CSVDuplicateURIErrorObject$`colIndex`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`header`)) {
        self$`header` <- CSVDuplicateURIErrorObject$`header`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`value`)) {
        self$`value` <- CSVDuplicateURIErrorObject$`value`
      }
      if (!is.null(CSVDuplicateURIErrorObject$`previousRow`)) {
        self$`previousRow` <- CSVDuplicateURIErrorObject$`previousRow`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "rowIndex": %s,
           "colIndex": %s,
           "header": %s,
           "value": %s,
           "previousRow": %s
        }',
        ifelse(is.null(self$`rowIndex`), "null",as.numeric(jsonlite::toJSON(self$`rowIndex`,auto_unbox=TRUE, null = "null"))),
        ifelse(is.null(self$`colIndex`), "null",as.numeric(jsonlite::toJSON(self$`colIndex`,auto_unbox=TRUE, null = "null"))),
        ifelse(is.null(self$`header`), "null",jsonlite::toJSON(self$`header`,auto_unbox=TRUE, null = "null")),
        ifelse(is.null(self$`value`), "null",jsonlite::toJSON(self$`value`,auto_unbox=TRUE, null = "null")),
        ifelse(is.null(self$`previousRow`), "null",as.numeric(jsonlite::toJSON(self$`previousRow`,auto_unbox=TRUE, null = "null")))
      )
    },
    fromJSONString = function(CSVDuplicateURIErrorJson) {
      CSVDuplicateURIErrorObject <- jsonlite::fromJSON(CSVDuplicateURIErrorJson)
      self$`rowIndex` <- CSVDuplicateURIErrorObject$`rowIndex`
      self$`colIndex` <- CSVDuplicateURIErrorObject$`colIndex`
      self$`header` <- CSVDuplicateURIErrorObject$`header`
      self$`value` <- CSVDuplicateURIErrorObject$`value`
      self$`previousRow` <- CSVDuplicateURIErrorObject$`previousRow`
    }
  )
)
